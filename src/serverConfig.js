const URL = ``;

export default {
  //Client api service
  All_Clients: URL + `AllClients`,
  ADD_Client: URL + `AddClient`,
  UPDATE_Client: URL + `UpdateClient`,
  DELETE_Client: URL + `DeleteClient`,
};
